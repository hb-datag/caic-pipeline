# Assets

Place the real CAIC intro clip here as `caic_intro.mp4`
(target format: 1080p, 30 fps, H.264 video, AAC audio — matching this lets
the stitcher use its fast stream-copy path).

Until it exists, Phase 4 generates a placeholder CAIC-blue title card
automatically, so the pipeline never blocks on this file.
